# Backend

Backend API dla aplikacji TV. **Osobne repozytorium** – ten folder ma własny git i jest synchronizowany z:

**https://github.com/7kar7son7/backend**

Push/ pull zawsze z tego folderu: `git push origin main`, `git pull origin main`.
